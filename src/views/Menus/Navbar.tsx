import React from 'react'
import "./Navbar.scss";

export interface Props {
    [key: string]: any
}

const Navbar = (props: Props) => {

    return (
        <div>navbar</div>
    );
}

 export default Navbar;